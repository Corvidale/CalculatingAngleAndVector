For this to work it uses its own class called Vector

the Vector has x and y value and can be accesed with .GetX() and .GetY()
currently i haven't adde .SetX and .SetY, because the program doesn't need it for my purposes
